@model Entities.Concrete.SecurityQuestion
@{
    ViewBag.Title = "SecurityQuestionUpdate";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="row text-center justify-content-center mainDiv">

    <div class="subDiv">
        <h2 class="securityQuestionUpdateHeaderText">Güvenlik Sorusu Güncelleme</h2>
        <div class="securityQuestionUpdateForm mt-5">
            @using (Html.BeginForm("SecurityQuestionUpdate", "Admin", FormMethod.Post, new { @autocomplete = "off" }))
            {
                <div class="col-md-12 mt-4">
                    <label class="form-label" style="float:left;">Id</label>
                    @Html.TextBoxFor(g => g.Id, new { @class = "form-control", @type = "text", @disabled = "disabled" })
                </div>
                <div class="col-md-12 mt-4">
                    <label for="securityQuestion" class="form-label" style="float:left;">Güvenlik Sorusu</label>
                    @Html.TextBoxFor(g => g.GuvenlikSorusu, new { @class = "form-control", @id = "securityQuestion", @type = "text", @required = "required", @maxlength = "50" })
                </div>
                <div class="text-center">
                    <a href="@Url.Action("SecurityQuestions","Admin")" class="btn btn-warning mt-4" style="width:110px;margin-right:10px;color:dimgray;">İptal</a>
                    <button type="submit" class="btn btn-primary mt-4">Güncelle</button>

                </div>
            }
        </div>
    </div>

</div>


@section CssFile{
    <link href="@Url.Content("~/Contents/AdminSecurityQuestionUpdate.css")" rel="stylesheet" type="text/css" /> }
