@model Entities.Concrete.SecurityQuestion
@{
    ViewBag.Title = "SecurityQuestionAdd";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="row text-center justify-content-center mainDiv">

    <div class="subDiv">
        <h2 class="securityQuestionAddHeaderText">Güvenlik Sorusu</h2>
        <div class="securityQuestionAddForm mt-5">
            @using (Html.BeginForm("SecurityQuestionAdd", "Admin", FormMethod.Post, new { @autocomplete = "off" }))
            {
                <div class="col-md-12 mt-4">
                    <label for="securityQuestionAdd" class="form-label" style="float:left;">Güvenlik Sorusu</label>
                    @Html.TextBoxFor(g => g.GuvenlikSorusu, new { @class = "form-control", @id = "securityQuestionAdd", @type = "text", @required = "required", @maxlength = "100" })
                </div>
                <div class="text-center">
                    <a href="@Url.Action("SecurityQuestions","Admin")" class="btn btn-warning mt-4" style="width:110px;margin-right:10px;color:dimgray;">İptal</a>
                    <button type="submit" class="btn btn-primary mt-4">Ekle</button>

                </div>
            }
        </div>
    </div>

</div>


@section CssFile{
    <link href="@Url.Content("~/Contents/AdminSecurityQuestionAdd.css")" rel="stylesheet" type="text/css" /> }

