@model Entities.Concrete.RentalSituation
@{
    ViewBag.Title = "VehicleStateAdd";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<div class="row text-center justify-content-center mainDiv">

    <div class="subDiv">
        <h2 class="vehicleStateAddHeaderText">Araç Durumu</h2>
        <div class="vehicleStateAddForm mt-5">
            @using (Html.BeginForm("VehicleStateAdd", "Admin", FormMethod.Post, new { @autocomplete = "off" }))
            {
                <div class="col-md-12 mt-4">
                    <label for="vehicleStateAdd" class="form-label" style="float:left;">Araç Durumu</label>
                    @Html.TextBoxFor(g => g.KiraDurumu, new { @class = "form-control", @id = "vehicleStateAdd", @type = "text", @required = "required", @maxlength = "40" })
                </div>
                <div class="text-center">
                    <a href="@Url.Action("VehicleStatus","Admin")" class="btn btn-warning mt-4" style="width:110px;margin-right:10px;color:dimgray;">İptal</a>
                    <button type="submit" class="btn btn-primary mt-4">Ekle</button>

                </div>
            }
        </div>
    </div>

</div>


@section CssFile{
    <link href="@Url.Content("~/Contents/AdminVehicleStateAdd.css")" rel="stylesheet" type="text/css" /> }

